package com.example.data.model.breed

data class BreedWithSubBreeds(
    val breedName: String,
    val subBreeds: List<SubBreed>,
    var isFavorite: Boolean = false,
    var isExpanded: Boolean = false
)

data class SubBreed(
    val name: String,
    var isFavorite: Boolean = false
)

fun getBreedsWithSubBreeds(breeds: Breeds): List<BreedWithSubBreeds> {
    return listOf(
        BreedWithSubBreeds("Affenpinscher", breeds.affenpinscher.map { SubBreed(it) }),
        BreedWithSubBreeds("African", breeds.african.map { SubBreed(it) }),
        BreedWithSubBreeds("Airedale", breeds.airedale.map { SubBreed(it) }),
        BreedWithSubBreeds("Akita", breeds.akita.map { SubBreed(it) }),
        BreedWithSubBreeds("Appenzeller", breeds.appenzeller.map { SubBreed(it) }),
        BreedWithSubBreeds("Australian", breeds.australian.map { SubBreed(it) }),
        BreedWithSubBreeds("Bakharwal", breeds.bakharwal.map { SubBreed(it) }),
        BreedWithSubBreeds("Basenji", breeds.basenji.map { SubBreed(it) }),
        BreedWithSubBreeds("Beagle", breeds.beagle.map { SubBreed(it) }),
        BreedWithSubBreeds("Bluetick", breeds.bluetick.map { SubBreed(it) }),
        BreedWithSubBreeds("Borzoi", breeds.borzoi.map { SubBreed(it) }),
        BreedWithSubBreeds("Bouvier", breeds.bouvier.map { SubBreed(it) }),
        BreedWithSubBreeds("Boxer", breeds.boxer.map { SubBreed(it) }),
        BreedWithSubBreeds("Brabancon", breeds.brabancon.map { SubBreed(it) }),
        BreedWithSubBreeds("Briard", breeds.briard.map { SubBreed(it) }),
        BreedWithSubBreeds("Buhund", breeds.buhund.map { SubBreed(it) }),
        BreedWithSubBreeds("Bulldog", breeds.bulldog.map { SubBreed(it) }),
        BreedWithSubBreeds("Bullterrier", breeds.bullterrier.map { SubBreed(it) }),
        BreedWithSubBreeds("Cattledog", breeds.cattledog.map { SubBreed(it) }),
        BreedWithSubBreeds("Cavapoo", breeds.cavapoo.map { SubBreed(it) }),
        BreedWithSubBreeds("Chihuahua", breeds.chihuahua.map { SubBreed(it) }),
        BreedWithSubBreeds("Chippiparai", breeds.chippiparai.map { SubBreed(it) }),
        BreedWithSubBreeds("Chow", breeds.chow.map { SubBreed(it) }),
        BreedWithSubBreeds("Clumber", breeds.clumber.map { SubBreed(it) }),
        BreedWithSubBreeds("Cockapoo", breeds.cockapoo.map { SubBreed(it) }),
        BreedWithSubBreeds("Collie", breeds.collie.map { SubBreed(it) }),
        BreedWithSubBreeds("Coonhound", breeds.coonhound.map { SubBreed(it) }),
        BreedWithSubBreeds("Corgi", breeds.corgi.map { SubBreed(it) }),
        BreedWithSubBreeds("Cotondetulear", breeds.cotondetulear.map { SubBreed(it) }),
        BreedWithSubBreeds("Dachshund", breeds.dachshund.map { SubBreed(it) }),
        BreedWithSubBreeds("Dalmatian", breeds.dalmatian.map { SubBreed(it) }),
        BreedWithSubBreeds("Dane", breeds.dane.map { SubBreed(it) }),
        BreedWithSubBreeds("Danish", breeds.danish.map { SubBreed(it) }),
        BreedWithSubBreeds("Deerhound", breeds.deerhound.map { SubBreed(it) }),
        BreedWithSubBreeds("Dhole", breeds.dhole.map { SubBreed(it) }),
        BreedWithSubBreeds("Dingo", breeds.dingo.map { SubBreed(it) }),
        BreedWithSubBreeds("Doberman", breeds.doberman.map { SubBreed(it) }),
        BreedWithSubBreeds("Elkhound", breeds.elkhound.map { SubBreed(it) }),
        BreedWithSubBreeds("Entlebucher", breeds.entlebucher.map { SubBreed(it) }),
        BreedWithSubBreeds("Eskimo", breeds.eskimo.map { SubBreed(it) }),
        BreedWithSubBreeds("Finnish", breeds.finnish.map { SubBreed(it) }),
        BreedWithSubBreeds("Frise", breeds.frise.map { SubBreed(it) }),
        BreedWithSubBreeds("Gaddi", breeds.gaddi.map { SubBreed(it) }),
        BreedWithSubBreeds("Germanshepherd", breeds.germanshepherd.map { SubBreed(it) }),
        BreedWithSubBreeds("Greyhound", breeds.greyhound.map { SubBreed(it) }),
        BreedWithSubBreeds("Groenendael", breeds.groenendael.map { SubBreed(it) }),
        BreedWithSubBreeds("Havanese", breeds.havanese.map { SubBreed(it) }),
        BreedWithSubBreeds("Hound", breeds.hound.map { SubBreed(it) }),
        BreedWithSubBreeds("Husky", breeds.husky.map { SubBreed(it) }),
        BreedWithSubBreeds("Keeshond", breeds.keeshond.map { SubBreed(it) }),
        BreedWithSubBreeds("Kelpie", breeds.kelpie.map { SubBreed(it) }),
        BreedWithSubBreeds("Kombai", breeds.kombai.map { SubBreed(it) }),
        BreedWithSubBreeds("Komondor", breeds.komondor.map { SubBreed(it) }),
        BreedWithSubBreeds("Kuvasz", breeds.kuvasz.map { SubBreed(it) }),
        BreedWithSubBreeds("Labradoodle", breeds.labradoodle.map { SubBreed(it) }),
        BreedWithSubBreeds("Labrador", breeds.labrador.map { SubBreed(it) }),
        BreedWithSubBreeds("Leonberg", breeds.leonberg.map { SubBreed(it) }),
        BreedWithSubBreeds("Lhasa", breeds.lhasa.map { SubBreed(it) }),
        BreedWithSubBreeds("Malamute", breeds.malamute.map { SubBreed(it) }),
        BreedWithSubBreeds("Malinois", breeds.malinois.map { SubBreed(it) }),
        BreedWithSubBreeds("Maltese", breeds.maltese.map { SubBreed(it) }),
        BreedWithSubBreeds("Mastiff", breeds.mastiff.map { SubBreed(it) }),
        BreedWithSubBreeds("Mexicanhairless", breeds.mexicanhairless.map { SubBreed(it) }),
        BreedWithSubBreeds("Mix", breeds.mix.map { SubBreed(it) }),
        BreedWithSubBreeds("Mountain", breeds.mountain.map { SubBreed(it) }),
        BreedWithSubBreeds("Mudhol", breeds.mudhol.map { SubBreed(it) }),
        BreedWithSubBreeds("Newfoundland", breeds.newfoundland.map { SubBreed(it) }),
        BreedWithSubBreeds("Otterhound", breeds.otterhound.map { SubBreed(it) }),
        BreedWithSubBreeds("Ovcharka", breeds.ovcharka.map { SubBreed(it) }),
        BreedWithSubBreeds("Papillon", breeds.papillon.map { SubBreed(it) }),
        BreedWithSubBreeds("Pariah", breeds.pariah.map { SubBreed(it) }),
        BreedWithSubBreeds("Pekinese", breeds.pekinese.map { SubBreed(it) }),
        BreedWithSubBreeds("Pembroke", breeds.pembroke.map { SubBreed(it) }),
        BreedWithSubBreeds("Pinscher", breeds.pinscher.map { SubBreed(it) }),
        BreedWithSubBreeds("Pitbull", breeds.pitbull.map { SubBreed(it) }),
        BreedWithSubBreeds("Pointer", breeds.pointer.map { SubBreed(it) }),
        BreedWithSubBreeds("Pomeranian", breeds.pomeranian.map { SubBreed(it) }),
        BreedWithSubBreeds("Poodle", breeds.poodle.map { SubBreed(it) }),
        BreedWithSubBreeds("Pug", breeds.pug.map { SubBreed(it) }),
        BreedWithSubBreeds("Puggle", breeds.puggle.map { SubBreed(it) }),
        BreedWithSubBreeds("Pyrenees", breeds.pyrenees.map { SubBreed(it) }),
        BreedWithSubBreeds("Rajapalayam", breeds.rajapalayam.map { SubBreed(it) }),
        BreedWithSubBreeds("Redbone", breeds.redbone.map { SubBreed(it) }),
        BreedWithSubBreeds("Retriever", breeds.retriever.map { SubBreed(it) }),
        BreedWithSubBreeds("Ridgeback", breeds.ridgeback.map { SubBreed(it) }),
        BreedWithSubBreeds("Rottweiler", breeds.rottweiler.map { SubBreed(it) }),
        BreedWithSubBreeds("Saluki", breeds.saluki.map { SubBreed(it) }),
        BreedWithSubBreeds("Samoyed", breeds.samoyed.map { SubBreed(it) }),
        BreedWithSubBreeds("Schipperke", breeds.schipperke.map { SubBreed(it) }),
        BreedWithSubBreeds("Schnauzer", breeds.schnauzer.map { SubBreed(it) }),
        BreedWithSubBreeds("Segugio", breeds.segugio.map { SubBreed(it) }),
        BreedWithSubBreeds("Setter", breeds.setter.map { SubBreed(it) }),
        BreedWithSubBreeds("Sharpei", breeds.sharpei.map { SubBreed(it) }),
        BreedWithSubBreeds("Sheepdog", breeds.sheepdog.map { SubBreed(it) }),
        BreedWithSubBreeds("Shiba", breeds.shiba.map { SubBreed(it) }),
        BreedWithSubBreeds("Shihtzu", breeds.shihtzu.map { SubBreed(it) }),
        BreedWithSubBreeds("Spaniel", breeds.spaniel.map { SubBreed(it) }),
        BreedWithSubBreeds("Spitz", breeds.spitz.map { SubBreed(it) }),
        BreedWithSubBreeds("Springer", breeds.springer.map { SubBreed(it) }),
        BreedWithSubBreeds("Stbernard", breeds.stbernard.map { SubBreed(it) }),
        BreedWithSubBreeds("Terrier", breeds.terrier.map { SubBreed(it) }),
        BreedWithSubBreeds("Tervuren", breeds.tervuren.map { SubBreed(it) }),
        BreedWithSubBreeds("Vizsla", breeds.vizsla.map { SubBreed(it) }),
        BreedWithSubBreeds("Waterdog", breeds.waterdog.map { SubBreed(it) }),
        BreedWithSubBreeds("Weimaraner", breeds.weimaraner.map { SubBreed(it) }),
        BreedWithSubBreeds("Whippet", breeds.whippet.map { SubBreed(it) }),
        BreedWithSubBreeds("Wolfhound", breeds.wolfhound.map { SubBreed(it) })
    )
}